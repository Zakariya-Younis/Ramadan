# تعليمات إعداد قاعدة البيانات والأسئلة

## الخطوة 1: تنفيذ Schema الجديد

1. اذهب إلى **Supabase Dashboard**: https://supabase.com/dashboard
2. اختر مشروعك: **tzrljvsvubosyyrbuqrn**
3. اذهب إلى **SQL Editor** (من القائمة الجانبية)
4. انقر على **New Query**
5. انسخ **كامل محتوى** ملف `supabase/schema.sql`
6. الصق في المحرر واضغط **Run** أو `Ctrl+Enter`

> [!IMPORTANT]
> هذا سيقوم بإنشاء الجداول الجديدة:
> - `users` (مع حقول is_banned, last_active, total_days_participated)
> - `questions` (مع difficulty, is_bonus, bonus_date)
> - `daily_sessions` (لتتبع الجلسات اليومية)
> - `user_answers` (لحفظ الإجابات)
> - `attempts` (للوحة المتصدرين)

## الخطوة 2: استيراد الأسئلة (150 سؤال)

1. في نفس **SQL Editor**
2. انقر على **New Query** مرة أخرى
3. انسخ **كامل محتوى** ملف `supabase/questions_data.sql`
4. الصق في المحرر واضغط **Run**

> [!NOTE]
> هذا سيضيف 150 سؤالاً دينياً:
> - 50 سؤال سهل (5 نقاط)
> - 50 سؤال متوسط (10 نقاط)
> - 47 سؤال صعب (15 نقطة)
> - 3 أسئلة بونص (20 نقطة) في أيام محددة

## الخطوة 3: التحقق من نجاح العملية

1. اذهب إلى **Table Editor** (من القائمة الجانبية)
2. اختر جدول `questions`
3. يجب أن ترى **150 سؤالاً**
4. تحقق من وجود الأعمدة الجديدة:
   - `difficulty` (easy, medium, hard)
   - `is_bonus` (true/false)
   - `bonus_date` (للأسئلة البونص)

## الخطوة 4: إنشاء أول مستخدم أدمن

1. سجل مستخدم جديد في التطبيق: http://localhost:3000/register
2. اذهب إلى **SQL Editor** في Supabase
3. نفذ هذا الأمر (استبدل البريد الإلكتروني):

```sql
UPDATE users 
SET role = 'admin' 
WHERE email = 'your-email@example.com';
```

4. سجل خروج ثم سجل دخول مرة أخرى

## الخطوة 5: اختبار النظام

### اختبار الاختبار اليومي:
1. اذهب إلى: http://localhost:3000/quiz
2. يجب أن ترى **3 أسئلة عشوائية**
3. شريط التقدم يظهر 3 دوائر
4. كل سؤال له مستوى صعوبة (سهل/متوسط/صعب)
5. بعد الإجابة على الثلاثة، تظهر النتيجة النهائية

### اختبار لوحة الأدمن:
1. اذهب إلى: http://localhost:3000/admin/questions
2. يجب أن ترى:
   - إحصائيات الأسئلة (50 سهل، 50 متوسط، 47 صعب، 3 بونص)
   - قائمة بجميع الأسئلة
   - إمكانية إضافة أسئلة جديدة
   - فلترة حسب المستوى

3. اذهب إلى: http://localhost:3000/admin/users
4. يجب أن ترى:
   - قائمة المستخدمين
   - إحصائيات (نشط/غير نشط/محظور)
   - إمكانية حظر/إلغاء حظر المستخدمين

## الخطوة 6: اختبار أسئلة البونص

أسئلة البونص مجدولة في التواريخ التالية:
- **2026-04-10** (يوم 10 رمضان): سؤال عن ليلة القدر
- **2026-04-20** (يوم 20 رمضان): سؤال عن العشرة المبشرين بالجنة
- **2026-04-27** (يوم 27 رمضان): سؤال عن غزوة الأحزاب

لاختبار سؤال البونص:
1. غيّر تاريخ `bonus_date` لأحد الأسئلة إلى اليوم:
```sql
UPDATE questions 
SET bonus_date = CURRENT_DATE 
WHERE is_bonus = true 
LIMIT 1;
```

2. أكمل الاختبار اليومي (3 أسئلة)
3. سيظهر سؤال البونص تلقائياً بعد الانتهاء

## المميزات الجديدة

### ✅ نظام الاختبار:
- 3 أسئلة عشوائية يومياً (واحد من كل مستوى)
- لا يمكن الحصول على نفس السؤال مرتين
- لا يمكن تحديث الصفحة للحصول على أسئلة جديدة
- شريط تقدم مع دوائر ملونة
- رسائل تحفيزية بين الأسئلة

### ✅ نظام النقاط:
- سهل: 5 نقاط
- متوسط: 10 نقاط
- صعب: 15 نقطة
- بونص: 20 نقطة
- الحد الأقصى اليومي: 30 نقطة (50 مع البونص)

### ✅ إدارة المستخدمين:
- تتبع النشاط (آخر تسجيل دخول)
- عدد الأيام المشاركة
- حظر/إلغاء حظر المستخدمين
- المستخدمون المحظورون لا يمكنهم الدخول للاختبار

## استكشاف الأخطاء

### لا تظهر الأسئلة؟
- تأكد من تنفيذ `questions_data.sql`
- تحقق من جدول `questions` في Table Editor

### لا يمكن الوصول لصفحات الأدمن؟
- تأكد من تحديث دور المستخدم إلى 'admin'
- سجل خروج ثم دخول مرة أخرى

### الأسئلة تتكرر؟
- هذا طبيعي إذا كان عدد الأسئلة قليل
- مع 150 سؤال، التكرار نادر جداً

### سؤال البونص لا يظهر؟
- تحقق من `bonus_date` في قاعدة البيانات
- يجب أن يكون التاريخ = اليوم الحالي
- يظهر فقط بعد إكمال الأسئلة الثلاثة العادية

## نصائح

> [!TIP]
> - يمكنك إضافة المزيد من الأسئلة من لوحة الأدمن
> - يمكنك تعديل تواريخ البونص حسب رغبتك
> - يمكنك حذف الأسئلة غير المناسبة
> - راقب نشاط المستخدمين من لوحة إدارة المستخدمين

**الآن كل شيء جاهز! 🎉**
